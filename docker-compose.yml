services:
  mongodb:
    image: mongo:7
    container_name: journal-mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: journal-app
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
